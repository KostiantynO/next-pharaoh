export const OPENED_SIDEBAR_WIDTH = 288;
export const FOLDED_SIDEBAR_WIDTH = 80;

// Assuming your map is 10 times larger than the visible area
export const MAP_SCALE = 100;
export const CENTER_TO_0 = 0.5;

export const GRID_SIZE = 50;
